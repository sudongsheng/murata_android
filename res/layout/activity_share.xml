<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:paddingLeft="16dp"
              android:paddingRight="16dp"
              android:background="@color/white">
    <ImageView android:id="@+id/share_img"
               android:layout_width="@dimen/share_img"
               android:layout_height="@dimen/share_img"
               android:layout_marginTop="28dp"
               android:layout_gravity="center_horizontal"/>
    <TextView android:layout_width="fill_parent" android:layout_height="wrap_content"
              android:layout_marginTop="28dp" android:textColor="@color/login_text"
              android:textSize="12sp"
              android:gravity="center_horizontal"
              android:text="@string/share_hint"/>
    <Button android:id="@+id/share_refresh"
            style="@style/login_btn_style"
            android:layout_marginTop="16dp"
            android:text="@string/refresh"/>
</LinearLayout>